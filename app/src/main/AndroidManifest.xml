<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.upc_pre_202401_cc238_ea_ventura_chancafe_eduardo_renato"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceder a Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Upcpre202401cc238EAVenturaChancafeEduardoRenato">

        <!-- Activity de Inicio (Launcher) -->
        <activity
            android:name=".activities.Home"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Upcpre202401cc238EAVenturaChancafeEduardoRenato">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FindPackageActivity -->
        <activity
            android:name=".activities.FindPackageActivity"
            android:exported="false"
            android:theme="@style/Theme.Upcpre202401cc238EAVenturaChancafeEduardoRenato" />

        <!-- FavoritePackagesActivity -->
        <activity
            android:name=".activities.FavoritePackagesActivity"
            android:exported="false"
            android:theme="@style/Theme.Upcpre202401cc238EAVenturaChancafeEduardoRenato" />

    </application>

</manifest>
